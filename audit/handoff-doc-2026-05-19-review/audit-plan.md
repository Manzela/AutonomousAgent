---
title: "Audit plan — patch HANDOFF-2026-05-19.md"
target: docs/superpowers/HANDOFF-2026-05-19.md
created: 2026-05-19
revised: 2026-05-19 (deep re-verification: P0-1 rescuable from stash; §5.2 refuted; §2 row 10 PARTIAL; +3 spec drifts)
related: ./findings.md
---

# Patch plan for HANDOFF-2026-05-19.md

Seven prioritized defects from `findings.md` after deep re-verification. **P0-1 has been re-scoped** — the missing audit directory is recoverable from `git stash@{0}`'s untracked-files sub-commit; rescue takes ~10 min (was ~1h to reconstruct from scratch). All other priorities stand. Each item has scope, file:line, evidence, and rough effort.

The doc is a single file under git — fix as one PR (`docs/fix(handoff)`) plus a separate **first-action** commit to rescue the stash before any `git gc`.

## P0-1 — `audit/phase1-to-phase2-readiness-2026-05-19/` is missing from disk but **rescuable from `git stash@{0}`** (TIME-CRITICAL)
**Why:** §11 + §5.3 + §6 all reference this directory. It does not exist on disk and was never committed to any named ref. Pass-1 + pass-2 said "never existed" — that's wrong. Deep re-verification found the entire directory (8 files including a 12,751-byte `SYNTHESIS.md`) inside the untracked-files sub-commit of `git stash@{0}` (commit `f2f8a6a037e3d89f8a958f539f2eeb224bb2a490`). The same stash also rescues `audit/phase1-completion-final-sweep-2026-05-18/{findings,audit-plan}.md` cited at line 130.

**The clock is ticking:** if the user runs `git stash drop stash@{0}` before rescue, the blobs become dangling. A subsequent `git gc --prune=now` deletes them permanently. **Rescue before any other docs work.**

**Where (handoff references that depend on the rescued directory):**
- `docs/superpowers/HANDOFF-2026-05-19.md:98` ("catalogued in `audit/phase1-to-phase2-readiness-2026-05-19/SYNTHESIS.md`")
- `docs/superpowers/HANDOFF-2026-05-19.md:124-129` (six bullets pointing at `audit/phase1-to-phase2-readiness-2026-05-19/*.md`)
- `docs/superpowers/HANDOFF-2026-05-19.md:130` (`-final-sweep-` variant of the 2026-05-18 dir; also rescuable from same stash)
- `docs/superpowers/HANDOFF-2026-05-19.md:255` ("Open `audit/phase1-to-phase2-readiness-2026-05-19/SYNTHESIS.md` for the master prioritized list.")

**Evidence:**
```
$ find audit -maxdepth 2 -type d
audit
audit/handoff-doc-2026-05-19-review
audit/phase1-completion-sweep-2026-05-18         # without -final-

$ git stash list
stash@{0}: On feat/wire-checkpoint-into-post-tool-call: wip-checkpoint-feat
...

# The standard `git stash show --name-only` only lists TRACKED changes.
# Use git ls-tree on the untracked-files sub-commit (f2f8a6a):
$ git ls-tree -r f2f8a6a | grep "phase1-to-phase2"
100644 blob 51439869...  audit/phase1-to-phase2-readiness-2026-05-19/SYNTHESIS.md
100644 blob 3ed625b7...  audit/phase1-to-phase2-readiness-2026-05-19/cicd-audit.md
100644 blob 18fa9c43...  audit/phase1-to-phase2-readiness-2026-05-19/coverage-combined.txt
100644 blob 9e9fee49...  audit/phase1-to-phase2-readiness-2026-05-19/coverage-unit.txt
100644 blob a5259b7e...  audit/phase1-to-phase2-readiness-2026-05-19/docs-architecture-audit.md
100644 blob 82a649f3...  audit/phase1-to-phase2-readiness-2026-05-19/observability-reliability-audit.md
100644 blob 937c7316...  audit/phase1-to-phase2-readiness-2026-05-19/quality-audit.md
100644 blob 191bce8d...  audit/phase1-to-phase2-readiness-2026-05-19/security-audit.md

$ git cat-file -s 51439869894cdf1bd7c7aa363d8a658b21938779
12751   # SYNTHESIS.md is 12.7 KB
```

**Fix (recommended — option A: rescue + commit):**

Step 1 — rescue (must run before any `git stash drop` / `git gc`):
```bash
cd "/Users/danielmanzela/RX-Research Project/AutonomousAgent"
git checkout -b chore/rescue-2026-05-19-audit-stash
git checkout f2f8a6a -- audit/phase1-to-phase2-readiness-2026-05-19/
git checkout f2f8a6a -- audit/phase1-completion-final-sweep-2026-05-18/
git add audit/phase1-to-phase2-readiness-2026-05-19/
git add audit/phase1-completion-final-sweep-2026-05-18/
git commit -m "chore(audit): rescue Phase 1→Phase 2 readiness audit from stash f2f8a6a

These artifacts were generated by 5 parallel Opus subagents during
Phase 1.0.1 hotfix work but never committed — they lived only in
'git stash@{0}' (untracked-files sub-commit). Surfaced by the
HANDOFF-2026-05-19 forensic audit. Includes SYNTHESIS.md (master
prioritized list) and 5 sub-reports referenced by §5.3, §6, §11."
gh pr create --title "chore(audit): rescue Phase 1→Phase 2 readiness audit from stash" --body "..."
```

Step 2 — only after the rescue PR merges:
- Verify each file renders and the SYNTHESIS contents match the §5.3 bullet list (sanity check that the stash isn't from an obsolete iteration).
- No edits to HANDOFF-2026-05-19.md are needed for §6, §11, or §5.3 line 98 — they now point at real files. Only patch line 130 (`-final-sweep-` is correct after rescue; the doc was right, the disk was wrong).

**Effort:** ~10 min rescue + ~15 min PR/CI + ~15 min sanity check = **~40 min total**, ~10 min of which is on the critical path before stash-drop risk.

**Fallback — option B (if stash rescue is undesired or fails):** the pass-2 plan to promote §5.3 to a new `audit/phase1.2-enterprise-sdlc/audit-plan.md` (~1h, see findings.md "P0-1" → "Fix options" → option 2 from pass 1). Use only if rescue is blocked.

**DO NOT** use the pass-1 option 1 (re-run the audit from scratch) — wasteful given the artifacts exist.

---

## P1-1 — §1 open-issues list names a nonexistent issue + omits the real open one
**Why:** §1 tells the next session that issue #88 should be manually closed because PR #63's body lacked a closing keyword. (a) Issue #88 doesn't exist; (b) PR #63's title contains `(closes #88)`. Meanwhile issue #50 ("AutonomousAgent is DOWN") is actually open and unmentioned. A fresh session will spend 5-10 minutes trying to find #88 in the dashboard before giving up.

**Where:** `docs/superpowers/HANDOFF-2026-05-19.md:24`:
```
| Open issues | 4 — `#53`, `#54`, `#55` (Phase 1.1 deferred), `#88` (rotation — should be closed; PR #63 didn't auto-close because the keyword wasn't in body. Manually close it.) |
```

**Evidence:**
```
$ gh issue list --state open
#53 Phase 1.1: emit OpenInference attributes ...
#54 Phase 1.1: wire Honcho persistent memory ...
#55 Phase 1.1: attach DB to LiteLLM proxy ...
#50 AutonomousAgent is DOWN              ← unmentioned in handoff
$ gh issue view 88
GraphQL: Could not resolve to an issue or pull request with the number of 88.
$ gh pr view 63 --json title
{"title":"chore(secrets): rotate 4 leaked credentials (closes #88)"}
```

**Fix:**
1. Close issue #50 first with a comment referencing PR #62 (escalation-watcher), PR #63 (rotation), and the §8 smoke verification (8/8 PASS). Effort: 5 min.
2. Patch the row to: `| Open issues | 3 — \`#53\`, \`#54\`, \`#55\` (Phase 1.1 deferred) |`. Effort: 1 min.
3. (Optional, if you want a paper trail for the future) Add a one-line note in §3 (PR #63 row): "Title contains `(closes #88)` referencing an issue that was never created — closing keyword is a dead link, not a malformed body."

**Effort:** ~10 minutes total.

---

## P2-1 — §7.3 says compose has BOTH `/root/.hermes` and `/home/hermes/.hermes` mounts; only the latter is live
**Why:** §7.3 directs the reader to "Read `deploy/docker-compose.yml` hermes service before assuming." A reader who does will find only `/home/hermes/.hermes` mounts, and only `/root/.hermes` *comments*. The handoff is mildly misleading and minorly undermines trust in the rest of §7.

**Where:** `docs/superpowers/HANDOFF-2026-05-19.md:155`:
> Our 6 plugins are mounted to `/root/.hermes/plugins/<name>` per PR #48 + path-rebased to `/home/hermes/.hermes/plugins/<name>` per PR #60 (non-root). **The compose has BOTH because of how the volume mounts overlay HOME.** Read `deploy/docker-compose.yml` hermes service before assuming.

**Evidence:** `grep -nE "/root/.hermes" deploy/docker-compose.yml` returns 4 hits, all on lines 215, 216, 219, 229 — every one inside the comment block explaining the PR #60 migration. The actual mount paths are exclusively `/home/hermes/.hermes`.

**Fix:** Replace the bolded sentence with: "The compose has *comments* referencing the legacy `/root/.hermes` paths (the PR #60 migration is documented inline at `deploy/docker-compose.yml:215-229`) but all *live mount paths* are `/home/hermes/.hermes`."

**Effort:** ~2 minutes.

---

## P1-2 — Phase 1 design spec contradicts live code (evaluator threshold)
**Why:** §6 cites `docs/superpowers/specs/2026-05-18-phase1-completion-coordination-design.md` as the canonical Phase 1 design and implies it's still authoritative. A fresh session reading it as gospel for any evaluator-adjacent Phase 1.1 work (e.g. issue #53 OpenInference attrs, which touches the observability/evaluators interplay) will write incorrect code.

**Specifics (from pass-2 spec-vs-code cross-check):**
- **Spec §4.2:** `accept_threshold: 3` (integer — count of judges)
- **Live code (`config/limits.yaml`, `lib/evaluators/consensus.py`):** `accept_threshold: 0.75` (float — percentage; comment in config says `# 75% (3 of 4 judges)`)
- The spec and the live code disagree on *type*, not just value. A fresh session implementing against the spec will write `if count >= accept_threshold:` instead of `if count / total >= accept_threshold:` — a real bug that won't be caught by unit tests built against the live config.

**Where:**
- `docs/superpowers/specs/2026-05-18-phase1-completion-coordination-design.md` §4.2 (evaluator section)
- `config/limits.yaml` (the live truth)
- `lib/evaluators/consensus.py` (the consumer)

**Fix options:**
1. Patch the spec to match live code (`accept_threshold: 0.75`, with note "Originally specced as integer count; rationalized to percentage during implementation"). Effort: 5 min.
2. Add a paragraph at the top of the spec: "**Drift notice 2026-05-19:** Live code has diverged from this spec in N places. See `config/limits.yaml` + `lib/evaluators/consensus.py` for authoritative values. This spec preserves design intent only." Effort: 5 min.
3. Add a `## Drift from spec` section to `HANDOFF-2026-05-19.md` §6 listing all known divergences, with the spec demoted from "still authoritative" to "design intent reference only". Effort: 15 min.

**Recommended:** option 3 (in the same docs PR as P0-1) — the spec is sufficiently behind reality that demoting it is more honest than patching individual values.

**Effort:** ~15 minutes.

**Other pass-2 spec/code divergences** (lower stakes; bundle the disclosure):
- **Phase β coordination protocol**: spec specified 3 parallel sessions (c/d/e) with per-session briefs converging on `phase/1-completion`; reality was a single consolidated PR #49 ("chore: complete Phase 1 — anchors + evaluators + durability + checkpointing + memory + kanban"). No precedent for the spec's parallel-session protocol exists in git history. Phase 1.1 issues are small enough to run serially, so this is documentation debt only.
- **Phase α-0 PR count**: spec described 4 isolation PRs (α-0.1 to α-0.4); reality is 8 PRs (#56-#63) bundled differently. All 4 spec-identified α-0 defects ARE fixed, just not as the spec's isolated PRs.

**Additional drifts found in deep re-verification** (bundle into the same `## Drift from spec` section):

**(d) `reject_threshold` — same int-vs-float drift as accept_threshold:**
- Spec line 188: `reject_threshold: 3` (int — count of judges)
- `config/limits.yaml:154`: `reject_threshold: 0.75` (float — percentage)
- `lib/evaluators/consensus.py:60,89`: `reject_threshold: float = 0.75` + `if reject_pct >= reject_threshold:`
- Fix bundled with (a) — same exact transformation.

**(e) `lib/anchors/__init__.py:55` citation is obsolete in 5 places (spec + plan):**
- Spec lines 304 + 401, Plan lines 1903 + 1944 + 1963 all cite `lib/anchors/__init__.py:55` as the location of a `TODO(P1-5)` stub for the `/cancel <id>` handler.
- Reality: anchors line 55 is now error-handling code inside `_resolve_storage_dir()` (`return _FALLBACK_STORAGE_DIR` in a try/except). The real `/cancel` handler is at line ~259 (`def _slash_cancel(raw_args: str) -> str:`). The `TODO(P1-5)` stub has **already been implemented** — the handler dispatches `arg → telegram_bridge.cancel_card(arg)` with proper success/failure messages.
- Risk to a fresh session: navigating to `lib/anchors/__init__.py:55` looking for a stub to fill will land on unrelated error-handling code; might either re-implement the already-implemented handler or get confused about whether the work is done.
- Fix: spec + plan need a line-number patch OR a top-of-file "ALL line numbers in this doc are pre-2026-05-18 hotfix; consult the actual file" warning.

**(f) Kanban DB path drift in spec + plan:**
- Spec line 304: mount `hermes-data:/root/.hermes/kanban` for SQLite persistence (i.e. a `kanban/` subdir under HOME=`/root`)
- Plan lines 1267-1269: default `KANBAN_DB_PATH = os.environ.get("HERMES_KANBAN_DB", "/root/.hermes/kanban/kanban.db")`
- Reality (post PR #60): HOME is `/home/hermes`, the DB lives at `/home/hermes/.hermes/kanban.db` (no `kanban/` subdir). All 4 production code paths agree:
  - `lib/durability/escalation.py:21` — `KANBAN_DB_PATH = ".../home/hermes/.hermes/kanban.db"`
  - `lib/kanban/telegram_bridge.py:57` — `_DEFAULT_DB_PATH = "/home/hermes/.hermes/kanban.db"`
  - `config/limits.yaml:188` — `db_path: /home/hermes/.hermes/kanban.db` (with explicit comment "NOT a kanban/ subdir; verified live")
  - `scripts/snapshot.sh:49-50` — `/home/hermes/.hermes/kanban.db`
- Risk to a fresh session: implementing against the spec's `/root/.hermes/kanban/kanban.db` will create a DB at the wrong path that won't be picked up by escalation or snapshot.

**(g) `lib/durability/escalation.py` — actually correct:**
- The pass-2 hand-off summary speculated this might be a drift; deep re-verification confirms spec line 214 is accurate. The file exists at the cited path with the cited purpose. **Not a drift.**

---

## P2-2 — §3 PR #61 title — git commit subject vs GitHub PR title disagree
**Why:** Tiny inconsistency. Pick one canonical phrasing.

**Where:** `docs/superpowers/HANDOFF-2026-05-19.md:58` quotes PR #61 as `feat(kanban): fill hook bodies + Telegram send + escalation path (#61)`. The git squash-commit subject matches that ("escalation path"); the GitHub PR title was rewritten to "path rename".

**Fix:** No action required — git history is the source of truth and matches the doc. Optional: parenthetical note `(PR title on GitHub was renamed to "path rename" after merge — git history retains the original)`.

**Effort:** ~30 seconds if you patch; ~0 if you accept the inconsistency.

---

## P2-3 — §5.3 has 2 minor numeric drifts (verified in pass 2)

**(a) README service count:** §5.3 says "README claims 12 services; reality is 7." Reality is **8** (7 long-running + 1 init sidecar that exits 0) — the §1 row "Live container state" correctly says 8. The §5.3 paraphrase is inconsistent with §1. README literally does say "twelve services" (verified `README.md:55`). Fix: "reality is 7" → "reality is 8 (7 long-running + 1 init sidecar)".

**(b) Hermes submodule commits-behind:** §5.3 says "718 commits behind upstream". Verified actual count is **715** (`git log --oneline ddb8d8f..62573f44c | wc -l = 715`). Off by 3. Immaterial to the recommendation (still need to bump), but if §5.3 is being promoted to the canonical catalog (per P0-1 option 2) the number should match reality. Fix: "718 commits" → "~715 commits".

**Effort:** ~2 minutes for both.

---

## P1-3 — §5.2 "Daily 4am session reset" config does not exist (REFUTED by deep re-verification)
**Why:** §5.2 bullet 3 says: "**Daily 4am session reset** — hostile to 48h long-running jobs that Phase 1 was designed to support. Configurable; should be disabled OR documented as a deliberate constraint. Find config: `grep -rn "daily.*reset\|session.*reset" config/ docs/`." A fresh session that runs the recommended grep gets **empty output** and will either chase a ghost or quietly drop the item from the work plan. §5.3 line 116 also lists "daily session reset" as one of "10 missing ADRs for post-Phase-1 decisions" — if the thing doesn't exist, no ADR is owed, and the count of missing ADRs should be 9 not 10.

**Where:** `docs/superpowers/HANDOFF-2026-05-19.md:96` (the §5.2 bullet) + `docs/superpowers/HANDOFF-2026-05-19.md:116` (the §5.3 ADR list entry).

**Evidence:**
```
$ grep -rn "daily.*reset\|session.*reset\|reset.*daily\|reset.*session\|reset.*4am\|reset.*04:" \
        config/ docs/ deploy/ 2>/dev/null
(no matches)

$ grep -rn "4am\|0 4 \* \*" config/ deploy/ 2>/dev/null
config/limits.yaml:46:  gcs_snapshot_cron: "0 4 * * *"
```

The only 04:00 cron in the codebase is `gcs_snapshot_cron` — a GCS object-storage snapshot job, NOT a session reset. A snapshot writes vault contents to Google Cloud Storage; it does not terminate or reset sessions.

**Most likely cause:** Either the "daily reset" was a feature that got designed out of Phase 1 and the handoff didn't get updated, OR the handoff author was thinking of a different system and confused it with this one.

**Fix options:**
1. **Remove the §5.2 bullet entirely** (5 min). Cleanest if no such reset is actually planned.
2. **Rewrite to reflect reality** (~5 min): "Daily 04:00 GCS snapshot (`gcs_snapshot_cron` in `config/limits.yaml:46`) runs `scripts/snapshot.sh`. Confirm this is intended behavior; should it be configurable to skip when a long-running job is mid-flight?"
3. **Investigate whether a reset exists elsewhere** (e.g. in Hermes submodule, in upstream Anthropic SDK config) — only worthwhile if the user remembers writing this functionality.

**Also patch §5.3 line 116:** remove "daily session reset" from the list of "10 missing ADRs" → revise count to 9 OR replace with a different post-Phase-1 decision that *does* need an ADR.

**Effort:** ~10 minutes for option 1 or 2 (preferred); decision on option 3 requires user input.

---

## P1-4 — §2 row 10 (Phoenix span emission) is currently NOT verifiable live → downgrade to PARTIAL
**Why:** §2 marks "`Phoenix emits turn.start / model.call / tool.dispatch from hermes-agent`" as ✅ with "(PR #52 + verified live spans)" as proof. Live verification today against the running Phoenix database returns **0 spans of any of those names**. All 36 spans in Phoenix are LiteLLM-emitted (gen_ai.*, llm.*, litellm.call_id) with `span_kind=UNKNOWN` and empty `service.name`. The code is wired correctly — `lib/observability/__init__.py:99,152,225` all `_tracer.start_span(...)` for the three expected names — and the Hermes container env is correct (`OTEL_SERVICE_NAME=hermes`, `OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318`). But no Hermes session appears to have fired since the most recent container restart (or there's a downstream forwarding break specific to Hermes spans).

**Where:** `docs/superpowers/HANDOFF-2026-05-19.md:40` (the §2 row).

**Evidence:**
```
$ curl -s 'http://localhost:6006/v1/projects/UHJvamVjdDox/spans?limit=500' \
    | jq '.data | map(.name) | group_by(.) | map({name:.[0], count:length})'
[{"name":"Received Proxy Server Request","count":6},
 {"name":"proxy_pre_call","count":6},
 {"name":"raw_gen_ai_request","count":6},
 {"name":"router","count":12},
 {"name":"self","count":6}]   # 36 total; 0 named turn.start, model.call, or tool.dispatch

$ grep -n "start_span" lib/observability/__init__.py
99:        span = _tracer.start_span("turn.start")
152:       span = _tracer.start_span("tool.dispatch")
188:       span = _tracer.start_span("tool.dispatch")
225:       span = _tracer.start_span("model.call")
```

**Fix options:**
1. **Trigger one Hermes session** before publishing the handoff and re-verify (`echo "test prompt" | docker exec -i hermes hermes run` or equivalent). If spans appear → row stays ✅. If they don't appear → there's a real OTel forwarding bug to file as a new issue.
2. **Downgrade row 10** to ⚠ with text: "Code wired and unit-tested (lib/observability/__init__.py:99,152,225); live span emission pending — Phoenix currently shows 0 of these span names. Re-verify after first real Hermes session post-restart."
3. **Both** (recommended): trigger one session, capture actual span output as proof in the handoff body, and tighten the verification command in §8 to include the curl query above.

**Effort:** ~5 min to trigger + 5 min to update the row.

---

## P2-4 — §1 vs §5.3 internal inconsistency on service count
**Why:** §1 row "Live container state" says **8 services**. §5.3 says **"README claims 12 services; reality is 7"**. Both are arithmetically right under different conventions (8 = defined services in compose; 7 = long-running services after volume-init exits). But the handoff using both numbers without explanation reduces reader trust and makes it harder to validate the §5.3 claim.

**Where:**
- `docs/superpowers/HANDOFF-2026-05-19.md` §1 row "Live container state" (`8 services`)
- `docs/superpowers/HANDOFF-2026-05-19.md:113` (`README claims 12 services; reality is 7`)

**Evidence:**
```
$ docker compose -f deploy/docker-compose.yml --project-directory deploy config --services | wc -l
8
$ docker compose ... ps --format '{{.Service}}' | wc -l
7
```

**Fix:** Pick one convention and apply it twice. Recommended: "8 defined services (7 long-running + `volume-init` init-sidecar that exits 0 after chown)" — apply to both §1 and §5.3. README's "twelve services" is still wrong by either count; the §5.3 bullet should read: "README claims 12 services; reality is 8 (7 long-running + 1 init sidecar)." (already covered in P2-3(a) — bundle with that fix.)

**Effort:** ~2 minutes (bundle into the P2-3(a) edit).

---

## Non-defects worth a one-line addition

These aren't fixes per se, but a fresh session would benefit from them being there. Bundle into the same PR or skip.

**N-1: Footnote that HEAD = `1a2c6fc` post the handoff's own PR #64**
Add after §1 row "`origin/main` HEAD": "*This row reflects HEAD at the time this handoff was drafted (before PR #64 merged). After #64 merges, `git rev-parse origin/main` will be `1a2c6fc` and `git log --oneline -1` will show `docs(handoff): Phase 1.0.1 + credential rotation handoff for next session (#64)`.*"

**N-2: §4 → §8: how to re-verify the rotated tokens without printing them**
Add to §8 (verification checklist) a short stanza like:
```bash
# Optional — re-verify rotated tokens (does not print them)
scripts/decrypt-secrets.sh                           # populates secrets/*.env at mode 0600
( . secrets/telegram.env && \
  curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getMe" | jq -r '.result.username' )
# Expected: Manzelagent_bot
```

**N-3: §7.1 — list all 5 durability submodules, not just 2**
The §7.1 list of "hook modules that follow the kwargs pattern" mentions `lib/durability/__init__.py` and `lib/durability/trichotomy.py` but the plugin has 5 submodules (`__init__`, `failure_matrix`, `trichotomy`, `escalation`, `checkpoint`, `resume`). A fresh session changing the hook signature contract needs to update all of them.

**Effort for all 3 nice-to-haves:** ~10 minutes total.

---

## Suggested PR strategy (revised post deep re-verification)

**Step 0 (TIME-CRITICAL, before any docs edits):** Rescue stash. Branch `chore/rescue-2026-05-19-audit-stash`, run the `git checkout f2f8a6a -- ...` commands from P0-1, merge. Do this **before any `git stash drop` or `git gc`** to avoid losing the SYNTHESIS.md + 5 sub-reports.

**Step 1 — single docs PR (recommended):** `docs(handoff): correct §1/§2/§5.2/§5.3/§7.3 + add re-verification stanza` bundling P1-1 + P1-3 + P1-4 + P2-1 + P2-3 + P2-4 + N-1/N-2/N-3. Closes issue #50 in the same PR (via `closes #50` in the body). One review cycle. The P0-1 disk-path defect resolves automatically once the rescue PR (Step 0) is merged.

**Step 2 — separate spec patch PR:** `docs(spec): mark Phase 1 coordination spec as design-intent + line-number drift notice`. P1-2 (all 7 sub-items a-g except (g) which is a non-drift). Effort ~30 min — discrete enough to keep out of the handoff PR.

**Step 3 — optional follow-up:** P2-2 (PR #61 title diff, cosmetic) — skip unless a doc revision is going to happen anyway.

**Why not bundle Step 0 + Step 1:** rescue is git-mechanical (no review needed); handoff edits need a careful review pass. Keep them separate so the rescue isn't gated on docs polish.

---

## What this audit does NOT touch

- The substantive Phase 1.0.1 work itself (PRs #56-#63). All verified, all working as the handoff claims. The §2 deliverables table is accurate.
- The §5.3 17-item enterprise-SDLC list itself. The items appear reasonable on inspection but pass 1 didn't independently verify each (e.g., did not run `gh api .../code-scanning/alerts` to confirm CodeQL is off, did not `git submodule status` to confirm the 718-commit Hermes lag, etc.). Pass 2 can do that, or skip if the catalog is going to be migrated to `audit/phase1.2-enterprise-sdlc/` anyway.
- The §9 sequencing recommendation. Forward-looking; not a fact to verify.

---

## To enrich in pass 2 (completed)

1. Cross-check the 17 enterprise-SDLC items in §5.3 against `audit/phase1-completion-sweep-2026-05-18/` to find any that were enumerated earlier — ✓ done. Most §5.3 items are net new; rescue-from-stash (P0-1 revised) is cheaper than reconstruction.
2. Run `git submodule status` + compare against `hermes-agent` upstream HEAD to confirm/refute the "718 commits behind, security-relevant atomic-writes fix at `62573f44c`" claim — ✓ done. Actual = 715 commits (off by 3); security commit verified.
3. Run `gh api repos/Manzela/AutonomousAgent/code-scanning/alerts` + `gh api .../dependabot/alerts` to confirm "Code Scanning + Secret Scanning + Vulnerability Alerts + Dependabot security fixes are all OFF" claim in §5.3 — ✓ done. All confirmed disabled (403/404).
4. Spot-check the §5.3 README/runbook drift claim ("README claims 12 services; reality is 7") by counting services in both `README.md` and `deploy/docker-compose.yml` — ✓ done. README says "twelve services" (line 55); reality is 7 long-running / 8 defined; §5.3 vs §1 numbers inconsistent — see P2-4.

## Done in deep re-verification

5. Full `git fsck --lost-found` sweep + stash-untracked-files inventory → recovered P0-1 directory in `f2f8a6a`.
6. Live Phoenix API query for `turn.start` / `model.call` / `tool.dispatch` span names → 0 hits → P1-4 downgrade.
7. Exhaustive grep for "daily reset" / "session reset" / 04:00 crons → only `gcs_snapshot_cron` found → P1-3 refutation.
8. Spec-vs-code drift sweep across `docs/superpowers/specs/*` + `docs/superpowers/plans/*` → 5 drifts confirmed (P1-2 expanded), 1 non-drift filtered out.
9. Telegram bot identity live-verified without printing token → `id=8911196639`, `username=Manzelagent_bot`, `first_name=AutonomousAgent` → matches §4's first-4 digest.
