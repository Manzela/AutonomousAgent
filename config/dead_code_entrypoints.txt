# dead_code_entrypoints.txt — Operator-approved escape hatch for dead-code-gate (C4)
#
# PURPOSE:
#   Symbols listed here are treated as runtime entrypoints by dead_code_gate.py,
#   even if no `if __name__ == "__main__":` block or pyproject [project.scripts]
#   entry is present. This allows operator-reviewed modules that are invoked by
#   external harnesses, Docker CMD, service meshes, or other runtime contexts
#   not visible to static analysis.
#
# RULES (ENFORCED):
#   1. Entries MUST be added by an operator in a SEPARATE commit from the code
#      being exempted (4-eyes principle — same as audit rubric changes).
#   2. Self-waivers (@manual, # dead-code: ignore, # noqa: deadcode) are
#      FORBIDDEN in the diff itself. This file is the ONLY allowed exemption path.
#   3. Each entry must include a comment explaining WHY it is an entrypoint.
#   4. Entries are one of:
#        module.dotted.path          — treats the whole module as an entrypoint root
#        module.dotted.path:symbol   — treats a specific symbol as a root
#
# FORMAT: one entry per line; '#' lines are comments; blank lines ignored.
#
# ---------------------------------------------------------------------------
# Known entrypoints seeded by operator review (2026-05-31):
#
# app/a2a_canary/main.py is a __main__-guarded module; it is listed here for
# documentation only (the gate already auto-detects __main__ guards). The Docker
# CMD ["python", "-m", "app.a2a_canary.main"] also launches it.
app.a2a_canary.main

# lib/a2a/server.py defines the A2A FastAPI ASGI app (`app = FastAPI(...)`) and is
# launched at runtime by `uvicorn lib.a2a.server:app --host 0.0.0.0 --port 9001`
# (deploy/docker-compose.yml:433). uvicorn imports the module, so its @app.post(...)
# route handlers (e.g. compatibility_admin_limits "/v1/admin/limits", compatibility_turn
# "/v1/turn") are framework-registered endpoints, not dead code — the gate just cannot
# see a compose-string uvicorn launch as an entrypoint. Operator-reviewed (2026-06-01).
lib.a2a.server

# lib/a2a/auth.py:verify_token is the JWT auth guard CALLED at lib/a2a/server.py:202
# (`identity = await verify_token(token_str, ...)`) inside the @app.post handler chain — it IS
# reachable via the lib.a2a.server uvicorn entrypoint. The callgraph cannot trace the
# FastAPI handler→helper await path once the stream/subscribe decorators carry response_model=None.
# Verified reachable (grep: server.py imports + calls it) 2026-06-01.
lib.a2a.auth:verify_token

# lib/durability/checkpoint.py:Checkpoint is INSTANTIATED at lib/durability/__init__.py:131
# (`cp = Checkpoint(...)`) on the durability resume path. Verified reachable 2026-06-01.
lib.durability.checkpoint:Checkpoint

# lib/durability/__init__.py:register is the plugin entrypoint invoked DYNAMICALLY by the
# hermes plugin loader (it imports the plugin module declared in plugin.yaml, then calls
# register(ctx) to wire the hooks: pre_tool_call / post_tool_call / on_session_start /
# on_session_end). All 7 lib/* plugins follow this identical contract (memory, a2a,
# observability, evaluators, kanban, anchors, durability); the AST callgraph cannot trace
# the loader's dynamic register(ctx) dispatch. SP-14 modified register() to wire the F34/F35
# detectors + trajectory shipper, making it a changed public symbol — it is genuinely
# reachable, not dead. Operator-reviewed (2026-06-01).
lib.durability:register

# app/core/trust.py is the C16/C17 trust primitive (C-15) imported DIRECTLY by
# lib/anchors/__init__.py (verified: line 13 `from app.core.trust import ...`).
# lib/anchors is a Hermes plugin loaded via the same dynamic register(ctx) dispatch
# as lib.durability, lib.memory, etc. (see lib.durability:register comment above).
# The AST callgraph cannot trace the plugin-loader path, so app.core.trust symbols
# appear unreachable even though they are called at runtime via:
#   plugin-loader → lib.anchors.register(ctx) → register_cli_command("new")
#   → _handle_new_cli (~L415) → _draft_from_intent (~L360)
#   → guard_action_class / tag_untrusted / UntrustedContent / ActionClass / TrustLevel
# NOTE: _on_pre_tool_call returns None (behind a TODO at ~L224) and does NOT call
# _draft_from_intent; the real live path is the `hermes new` CLI command above.
# Verified reachable (grep: lib/anchors/__init__.py L13-19 imports, L415-436 _handle_new_cli,
# L360-412 _draft_from_intent calls guard_action_class + tag_untrusted). Operator-reviewed (2026-06-01).
app.core.trust

# app/adapters/gcp/sandbox.py:FirecrackerSandbox is an intentionally-UNWIRED SP-05 scaffold:
# an AbstractSandbox subclass (GCP sandbox tier) whose __init__/run() raise NotImplementedError
# until SP-05 builds the orchestrator sandbox-selection that will instantiate it. It is kept per
# the CLAUDE.md builder-agent rule (keep the GCP subclass as a sibling; do NOT delete). H-06
# corrected its is_production_grade flag (True->False), making it a changed public symbol the C4
# gate checks; it has no runtime caller YET. Listed here as an operator-reviewed waiver — not a
# live entrypoint, but legitimately-present-not-yet-wired code that must not be deleted. REMOVE
# this line once SP-05 wires FirecrackerSandbox (it becomes genuinely reachable then).
app.adapters.gcp.sandbox:FirecrackerSandbox

# app/adapters/gcp/cloud_run_sandbox.py:CloudRunJobSandbox is the SP-05c production sandbox
# adapter. It is an AbstractSandbox subclass injected at runtime via SpineRunner(sandbox=
# CloudRunJobSandbox(...)); the static AST callgraph cannot trace this injection. Listed here
# as an operator-reviewed waiver — wired via runtime injection, not a static call site.
# REMOVE once a static call site (e.g. SpineRunner.configure_cloud_run_sandbox()) is added.
app.adapters.gcp.cloud_run_sandbox:CloudRunJobSandbox

# lib.scrubber.ScrubFilter is installed on the ROOT logger via
# lib/observability/otel_setup.py:310 (root.addFilter(ScrubFilter())); its
# .filter() is invoked by the Python logging framework's per-record dispatch —
# a runtime entrypoint the AST callgraph cannot trace (the import is local to
# setup_json_logging()). The scrubber PR adds exc_text scrubbing to .filter,
# making it a changed public symbol; this declares the genuine entrypoint so C4
# stops flagging ScrubFilter / ScrubFilter.filter as unreachable.
lib.scrubber:ScrubFilter

# scripts/ci/* are all __main__-guarded scripts; they auto-detect themselves.
# Listed here for discoverability only.
# scripts.ci.dead_code_gate    (auto-detected via __main__)
# scripts.ci.import_hygiene    (auto-detected via __main__)

# SP-01 LangGraph spine (walking skeleton) — the P0 control-plane TRUNK. build_spine()
# + SpineRunner are THE control plane a future FastAPI lifespan invokes (spec §2/§3:
# "checkpointer injected at FastAPI lifespan"); that runner-wiring layer is deliberately
# DEFERRED (no FastAPI app exists in app/ yet), so the AST callgraph has no runtime
# entrypoint to trace from. The spine is genuinely exercised by tests/unit/test_graph_spine.py
# (32 red-green oracles) and ~11 downstream items (SP-02/05/06/11/13/16/27/R1/R7) inherit
# its SpineState schema + node contracts. Listed as legitimately-present-not-yet-wired
# foundation that must not be deleted (same pattern as app.core.trust + FirecrackerSandbox).
# Pending operator review at PR. REMOVE each line once the FastAPI lifespan / downstream
# layer wires it (it becomes genuinely reachable then).
app.core.graph_state
app.core.checkpointer
app.adapters.inmemory.checkpointer
app.core.decision_record
app.core.graph
app.core.spine_runner

# app/main.py is the FastAPI service entrypoint (Pattern B fix). It assembles
# SpineRunner with production adapters and exposes the spine as an HTTP service.
# Launched at runtime by `uvicorn app.main:app --host 0.0.0.0 --port 8000`;
# the static AST callgraph cannot trace a uvicorn launch string as an entrypoint
# (identical limitation to lib.a2a.server above). Its @app.post() route handlers
# (start_goal, resume_thread, panic, rollback, healthz) are framework-registered
# endpoints, not dead code. Operator-reviewed 2026-06-04.
app.main

# SP-02 goal→DAG decomposition (TaskGraph validation + TaskSpec→TaskRequest phase bridge).
# app/core/decompose.py is the AbstractDecomposer seam + the pure SP-02 validators
# (validate_taskgraph / assert_decomposition_fidelity / is_catch_all_glob / acceptance_indices)
# + the task_graph_to_requests phase bridge; app/adapters/inmemory/decompose.py is the
# DETERMINISTIC rule-based InMemoryDecomposer CI runs against (no LLM); app/adapters/gcp/
# decompose.py:VertexDecomposer is the DEFERRED Vertex/LLM concretion stub (raises
# NotImplementedError, kept per the CLAUDE.md builder-agent rule). These are genuinely
# exercised by tests/unit/test_decompose.py (31 red-green oracles) and inherited by SP-05
# (sandbox manifest = node allowed_paths), SP-06 (scope-root gate over allowed_paths) and
# SP-11 (ready-node fan-out over the depends_on DAG), but the spine (app/core/graph.py)
# does NOT call the decomposer yet — clarify/decompose is the deferred SP-01 layer. Listed
# as legitimately-present-not-yet-wired foundation that must not be deleted (same pattern as
# the SP-01 spine + FirecrackerSandbox). Pending operator review at PR. REMOVE each line once
# the spine's decompose node wires it (it becomes genuinely reachable then).
app.core.decompose
app.adapters.inmemory.decompose
app.adapters.gcp.decompose

# SP-05 (F-1) per-node workspace. app/core/workspace.py:WorkspaceSession is GENUINELY WIRED
# and exercised — the execute spine node (app/core/graph.py) calls WorkspaceSession.create()
# to materialise a real git worktree, runs the applier inside it, and extracts the git diff
# (16 red-green oracles in tests/unit/test_sp05_workspace_diff.py drive it end-to-end). It is
# listed here ONLY because the AST callgraph cannot trace reachability INTO the spine's node
# closures: build_spine() calls the PRIVATE _build_nodes(), whose returned closures (execute,
# clarify, decompose, ...) are not in the gate's public-symbol reference index, so every
# symbol a closure references appears unreachable — the IDENTICAL limitation that keeps
# app.adapters.inmemory.decompose (used in the decompose closure) and app.core.spine_runner
# above on this list. NOT not-yet-wired; AST-untraceable. (app/core/_workspace_apply.py is
# reachable via its own __main__ block, so it is NOT listed.)
app.core.workspace

# SP-03 clarification driver + spec-drafter (PRD §6 EPIC-1 SP-03). NET-NEW hybrid
# adapter triple + driver:
#   app/core/spec_drafter.py            — AbstractSpecDrafter ABC + DraftResult /
#                                         ClarifyingQuestion / AppliedStandard /
#                                         Assumption / AmbiguityItem value objects.
#   app/adapters/inmemory/spec_drafter.py — InMemorySpecDrafter (deterministic, CI).
#   app/adapters/gcp/spec_drafter.py    — VertexSpecDrafter (DEFERRED stub, raises
#                                         NotImplementedError; kept as a sibling per
#                                         the CLAUDE.md builder-agent rule).
#   lib/anchors/clarification_driver.py — run_clarification_round + ClarificationOutcome.
#
# WIRED 2026-06-03 (audit F-3): the SP-03 clarify loop is now on the live spine —
# app/core/graph.py imports InMemorySpecDrafter + run_clarification_round and drives them
# from the `clarify` node (goal_intake → clarify ⇄ → sign_off). So app.core.spec_drafter,
# app.adapters.inmemory.spec_drafter, and lib.anchors.clarification_driver are now
# genuinely reachable via the graph and have been REMOVED from this allowlist (per the
# header rule "REMOVE each line once [...] they become genuinely reachable").
#
# STILL DEFERRED / present-not-yet-wired (kept):
#   - app.adapters.gcp.spec_drafter:VertexSpecDrafter — the Vertex structured-output
#     concretion (raises NotImplementedError); the spine injects InMemory in CI, Vertex
#     in prod once it lands. Sibling kept per the CLAUDE.md builder-agent rule.
#   - lib.anchors:run_clarification — the hermes plugin entry point (lib/anchors/__init__.py
#     register(ctx) → run_clarification). graph.py wires clarification_driver DIRECTLY
#     (run_clarification_round), so the loader's dynamic register(ctx) dispatch to
#     run_clarification is still untraceable by the AST callgraph until the session-aware
#     Hermes pre_tool_call adapter lands. Genuinely exercised by the SP-03 oracles.
app.adapters.gcp.spec_drafter:VertexSpecDrafter
lib.anchors:run_clarification

# DeepEval judge adapters (Cluster A) — keep the GEval/HallucinationMetric LLM-as-judge
# OFF OpenAI. DeterministicDeepEvalJudge is the hermetic CI default (fills the metric's
# structured schema deterministically); LiteLLMVertexJudge is the live cross-vendor
# Gemini-via-Vertex judge (staging only, DEEPEVAL_JUDGE=vertex); make_ci_judge() selects.
# Consumed by tests/integration/test_deepeval_trajectory.py (the LLM-judge eval tests) and
# by the DEFERRED SP-06 DAGMetric LLM leaf (app/core/eval_gate.py, not yet wired into a
# runtime path). The AST callgraph cannot see test consumers / the deferred leaf, so these
# read as unreachable. REMOVE once the SP-06 LLM leaf wires make_ci_judge() at runtime.
# Operator-reviewed 2026-06-03.
lib.evaluators.deepeval_judges
lib.evaluators.deepeval_judges:make_ci_judge
lib.evaluators.deepeval_judges:DeterministicDeepEvalJudge
lib.evaluators.deepeval_judges:LiteLLMVertexJudge
# scripts.ci.license_dep_gate  (auto-detected via __main__)
# scripts.ci.run_acceptance    (auto-detected via __main__)

# SP-R2 per-graph budget primitive (lib/durability/graph_budget.py) — aggregate_spend +
# budget_verdict are called ONLY from the app/core/graph.py fan_out CLOSURE (nested in the
# private _build_nodes; the AST callgraph cannot trace into closures — the same limitation that
# lists run_clarification + the InMemory/Vertex adapters above). GraphBudgetVerdict is constructed
# only inside budget_verdict. All three are genuinely exercised by the 18 SP-R2 oracles
# (tests/unit/test_graph_budget.py + test_sp_r2_budget_preempt.py) and live in the fan_out budget
# admission/enforcement path. Unlike GlobalThreadCap/BranchLease (also closure-used) these have NO
# module-level reference in _build_nodes' body, so they read as unreachable. Operator-reviewed 2026-06-03.
lib.durability.graph_budget:aggregate_spend
lib.durability.graph_budget:budget_verdict
lib.durability.graph_budget:GraphBudgetVerdict

# SP-16 AbstractBoard kanban projection seam (C14 gate-derived `done`).
# app/core/board.py = the AbstractBoard ABC + Card + project_plan (DAG->cards); the C14 invariant
# (the public API refuses agent-initiated `done`; done only via mark_done(gate=...)) lives in its
# CONCRETE methods. app/adapters/inmemory/board.py = InMemoryBoard, the hermetic store. The prod
# Hermes `kanban_db` adapter + the real `gh pr checks --required` GateReader are DEFERRED siblings.
# WIRED (slice 2): SpineRunner injects an InMemoryBoard -> build_spine -> the decompose node calls
# project_plan and fan_out calls board.set_status. These calls live INSIDE the spine-node CLOSURES
# (decompose/fan_out, nested in the private _build_nodes), which the AST callgraph CANNOT trace into
# (the identical limitation behind lib.durability.graph_budget + app.adapters.inmemory.decompose
# above), so project_plan/Card/BoardProjection/the InMemoryBoard methods still read as unreachable
# even though they are genuinely exercised — by the spine flow AND tests/unit/test_sp16_board.py +
# test_sp16_board_wiring.py. Operator-reviewed at PR. (NOT not-yet-wired; AST-untraceable.)
# Post-audit: SYMBOL-level (not whole-module) so C4 still bites any FUTURE dead symbol added to
# these two safety-relevant modules (a bare-module entry auto-roots every top-level public symbol).
# The listed symbols' public METHODS (set_status/mark_done/create_card, etc.) are wired via the
# gate's class->method edge during BFS, so this is no weaker for the genuinely-used surface.
app.core.board:AbstractBoard
app.core.board:Card
app.core.board:BoardProjection
app.core.board:BoardError
app.core.board:GateNotPassedError
app.core.board:GateReader
app.core.board:project_plan
app.adapters.inmemory.board:InMemoryBoard
app.adapters.inmemory.board:AlwaysReadyGate

# SP-R3 SqliteFileCheckpointer — the vendor-lock fallback adapter. Follows the same
# pattern as app.adapters.inmemory.checkpointer: the FastAPI lifespan (deferred) selects
# the checkpointer based on env config (InMemory for CI, Sqlite for fallback, Postgres
# for prod). The AST callgraph cannot trace the env-conditional lifespan selection
# (same limitation as app.core.spine_runner above). Operator-reviewed 2026-06-04.
app.adapters.inmemory.file_checkpointer

# SP-05d WorkspaceReaper + reap_orphaned_worktrees — called from SpineRunner.__init__
# (app/core/spine_runner.py). Both symbols are genuinely reachable: WorkspaceReaper is
# instantiated in __init__, and reap_orphaned_worktrees() is called in the same scope.
# The AST callgraph cannot trace through class-method bodies when the entrypoint is the
# module root (the identical limitation behind app.core.spine_runner being listed above).
# _default_reaper() is a factory helper used only by tests that bypass SpineRunner; it
# is not a dead symbol — it is the intended test-override entry point for the reaper.
# Operator-reviewed 2026-06-03.
app.core.reaper

# SP-12 autonomous CI/CD self-heal — SelfHealEvent is the repository_dispatch
# ci-failure payload parser. It is consumed by the future self-heal agent handler
# (a repository_dispatch listener — deferred SP-12-INT-3) and is imported from
# tests only today. The AutoMergePolicy / CheckResult / AutoMergeDecision symbols
# ARE reachable via scripts/ci/auto_merge_gate.py (__main__-guarded). SelfHealEvent
# is the only un-wired symbol; listed here as a present-not-yet-wired sibling
# (same pattern as app.adapters.gcp.decompose:VertexSpecDrafter). REMOVE once
# a runtime listener (FastAPI webhook / Lambda / Cloud Run handler) imports it.
# Operator-reviewed 2026-06-04.
app.core.ci_self_heal:SelfHealEvent

# SP-26 deployment rollback — Cloud Run revision traffic retargeting.
# app/adapters/gcp/cloud_run_revisions.py:CloudRunRevisionRollback is the GCP production
# concretion injected at runtime via SpineRunner(revision_rollback=CloudRunRevisionRollback(...)).
# require_revision_rollback() references AbstractRevisionRollback (C4-reachable), but the static
# callgraph cannot trace that the concrete GCP subclass is the injected type. The
# app/adapters/inmemory/rollback.py:RecordingRevisionRollback is the hermetic test double;
# it is instantiated only in tests, not at a static entrypoint. Both are legitimately-present
# adapters following the CLAUDE.md builder-agent pattern (abstract in core/, GCP in adapters/gcp/,
# in-memory in adapters/inmemory/). REMOVE each line once a static call site exists.
# Operator-reviewed 2026-06-04.
app.adapters.gcp.cloud_run_revisions:CloudRunRevisionRollback
app.adapters.inmemory.rollback:RecordingRevisionRollback

# SP-R4 checkpoint retention + judge_events TTL rotation.
# app/core/checkpoint_retention.py: gc_checkpoints() + find_threads_older_than() are
#   called from maintenance/teardown paths that are not yet wired to a runtime entrypoint
#   the AST callgraph can trace — same pattern as app.core.reaper above. These are
#   genuinely new production functions, not test helpers; they will be called by
#   SpineRunner teardown and operator maintenance scripts. REMOVE once a runtime wiring
#   exists (a FastAPI lifespan shutdown hook or a scheduled maintenance task).
# lib/evaluators/judge_events_retention.py: rotate() is called from the same class of
#   deferred maintenance contexts; judge_events.py itself is wired (lib.evaluators.judge_events
#   is already a production path) but the rotator is called by an operator/nightly task,
#   not inline with the hot path. Listed together with the checkpoint retention module
#   because they share the same deferred-maintenance pattern. Operator-reviewed 2026-06-04.
app.core.checkpoint_retention
lib.evaluators.judge_events_retention
# SP-J1 judge-calibration library — called by the deferred nightly calibration cron
#   (SP-J1-INT-1) and the SP-06 quarantine-check integration (SP-J1-INT-3). Not yet
#   wired to a runtime entrypoint the AST callgraph can trace. Module-level entry covers
#   top-level symbols; method-level entries added for nested methods/properties.
#   Operator-reviewed 2026-06-04.
lib.evaluators.judge_calibration
lib.evaluators.judge_calibration:CalibrationResult.passes
lib.evaluators.judge_calibration:JudgeCalibrationMonitor.score
lib.evaluators.judge_calibration:JudgeQuarantineRegistry.quarantine
lib.evaluators.judge_calibration:JudgeQuarantineRegistry.is_quarantined
lib.evaluators.judge_calibration:JudgeQuarantineRegistry.quarantined_ids

# lib/evaluators/dag_metric.py:DAGMetric is the evaluators metric for DAG conformance.
# It is used in eval tests and evaluation gates (C14 / C15) and is not called from
# the main FastAPI runtime entrypoints directly.
lib.evaluators.dag_metric:DAGMetric
