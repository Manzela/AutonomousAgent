receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 5s
    send_batch_size: 512
  memory_limiter:
    check_interval: 5s
    limit_percentage: 80
    spike_limit_percentage: 25
  resource:
    attributes:
      - key: deployment.environment
        value: prod
        action: upsert
  tail_sampling:
    decision_wait: 30s
    num_traces: 100000
    expected_new_traces_per_sec: 100
    policies:
      # Primary error policy: OTel span status explicitly set to ERROR.
      - name: errors
        type: status_code
        status_code: {status_codes: [ERROR]}
      # P3-7: Secondary error policy catches traces where the error is signalled
      # via a span attribute (error=true / error=1) rather than via the OTel
      # span status code — e.g., LiteLLM proxy spans that set the attribute but
      # don't call span.set_status(StatusCode.ERROR). Prevents the status_code
      # policy from silently dropping such traces.
      - name: error-attribute
        type: string_attribute
        string_attribute:
          key: error
          values: ["true", "1"]
          enabled_regex_matching: false
          cache_max_size: 1000
          invert_match: false
      - name: slow
        type: latency
        latency: {threshold_ms: 5000}
      - name: probabilistic
        type: probabilistic
        probabilistic: {sampling_percentage: 10}

exporters:
  googlecloud:
    project: autonomous-agent-2026
    log:
      default_log_name: "opentelemetry.io/collector-exported-log"

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, resource, tail_sampling, batch]
      exporters: [googlecloud]
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, resource, batch]
      exporters: [googlecloud]
    logs:
      receivers: [otlp]
      processors: [memory_limiter, resource, batch]
      exporters: [googlecloud]
