name: durability
version: 0.1.0
description: "P1-6 failure-matrix retry, P1-3 checkpoint resume, P1-4 REJECTED-inject (ordered on_session_start)"
kind: standalone
provides_hooks: [pre_tool_call, post_tool_call, on_session_start]
